-- direnv.vim integration
-- Automatically loads direnv environments when opening files
-- This ensures LSPs get the correct environment (e.g., for flox environments)
return {
  "direnv/direnv.vim",
}
